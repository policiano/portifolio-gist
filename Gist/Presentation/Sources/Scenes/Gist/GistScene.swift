import Foundation

enum Gist {
    enum GetDetails {
        struct Request { }

        typealias ViewModel = GistTableViewController.ViewModel
    }

    enum Bookmark {
        struct Request { }

        typealias ViewModel = GistTableViewController.ViewModel
    }
}
