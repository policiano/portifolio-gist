import Foundation

enum GistDetails {
    enum GetDetails {
        struct Request { }

        typealias ViewModel = GistDetailsTableViewController.ViewModel
    }

    enum Bookmark {
        struct Request { }

        typealias ViewModel = GistDetailsTableViewController.ViewModel
    }
}
